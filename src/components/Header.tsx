import React from 'react'
import '../styles/header.scss'

interface Props {

}

export const Header = (props: Props) => {
    return (
        <div className="header">
            Header
        </div>
    )
}
